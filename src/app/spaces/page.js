import styles from "./page.module.css"

export default async function SpacesPage(params) {
    return (
        <div className={styles.page}>
            <h2>Working Space Page</h2>
        </div>
    );
}